import React from 'react';

import { storiesOf } from '@storybook/react';

import CardAutocomplete from '.';

storiesOf('CardAutocomplete', module).add('CardAutocomplete', () => (
  <CardAutocomplete />
));
